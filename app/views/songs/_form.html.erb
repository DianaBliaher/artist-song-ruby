<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <%= form.fields_for :artist do |artist|%>
<div class="artist_fields">
  <div class="fiels">
    <%= artist.label :first_name %>
    <%= artist.text_field :first_name %><br>
  </div>
  <div class="fiels">
    <%= artist.label :last_name %>
    <%= artist.text_field :last_name %><br>
  </div>
  <div class="fiels">
    <%= artist.label :genre %>
    <%= artist.text_field :genre %><br>
  </div>
</div>
<% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
